# Core runtime (the counting logic) needs only numpy.
numpy>=1.24

# Config loading + tests.
PyYAML>=6.0
pytest>=7.0

# --- Real sensor adapters (install per deployment, not required for tests) ---
# pyzed        # Stereolabs ZED SDK Python bindings (install via ZED SDK)
# depthai      # Luxonis OAK-D
# onnxruntime  # if running a neural head-confirmation model at the edge
