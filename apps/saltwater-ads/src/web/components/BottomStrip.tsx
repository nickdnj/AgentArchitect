// UXD §3 — bottom status pill strip on Generate screen.
// TODO: live updates (poll every 5s per SAD §13 — recommend polling over WebSocket).

export function BottomStrip(): JSX.Element {
  return (
    <footer className="bottom-strip">
      {/* status pills land here */}
    </footer>
  );
}
