# {{PROJECT_NAME}}

Software project, team: {{TEAM_NAME}}. Provisioned by AgentArchitect ({{DATE}}).

Start work: `claude` in this directory. New product? Say "let's write the PRD" — the {{TEAM_SKILL}} team runs requirements → architecture → UX → dev plan → build.
