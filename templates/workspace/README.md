# {{TEAM_NAME}} Workspace

Standing workspace for the **{{TEAM_NAME}}**, provisioned by AgentArchitect ({{DATE}}).

- Start work: `claude` in this directory, then just ask — requests route through the team orchestrator.
- `outputs/` — finished work products. `workspace/` — working files and briefings.
- Agents/skills under `.claude/` are generated from the factory. Refresh: `aa sync .`
